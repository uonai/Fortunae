export default class IncomeItem {
  constructor(id, category, title, amount, type) {
    this.id = id;
    this.category = category;
    this.title = title;
    this.amount = amount;
    this.type = type;
  }
}
